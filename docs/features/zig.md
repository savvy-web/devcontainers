# Zig Compiler

Installs the Zig programming language globally.

## Options

- `zigVersion`: Zig version to install (e.g., 0.12.0, master). Default: `0.12.0`

## Usage

Add this feature to your `devcontainer.json` to install Zig globally.
