# Claude Code Agent (Global)

Installs the Claude Code CLI agent globally using the official native installer.

## Options

- `version`: Claude Code CLI version (use 'latest' for the newest release). Default: `latest`

## Usage

Add this feature to your `devcontainer.json` to install the Claude Code agent globally.
