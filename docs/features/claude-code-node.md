# Claude Code Agent (Node.js)

Installs Claude Code agent and CLI for Node.js environments.

## Options

- `version`: Claude Code agent version (absolute or 'latest'). Default: `latest`

## Usage

Add this feature to your `devcontainer.json` to install the Claude Code agent for Node.js.
