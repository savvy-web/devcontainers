# Claude Code Agent (claude-code)

Installs the Claude Code CLI agent globally via the official native installer (curl | bash). Does not require Node.js or npm.

## Example Usage

```json
"features": {
    "ghcr.io/savvy-web/claude-code:0": {}
}
```

## Usage

After the container starts, run `claude` to launch the agent.

---

_Note: This file was auto-generated from the [devcontainer-feature.json](https://github.com/savvy-web/devcontainers/blob/main/src/claude-code/devcontainer-feature.json). Add additional notes to a `NOTES.md`._
