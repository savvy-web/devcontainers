{
	"id": "outbound-firewall",
	"version": "0.1.0",
	"name": "Outbound Firewall",
	"description": "Configures outbound firewall rules for Codespaces and devcontainers.",
	"documentationURL": "https://github.com/savvy-web/devcontainers/blob/main/docs/features/outbound-firewall.md",
	"options": {
		"blockAll": {
			"type": "boolean",
			"default": false,
			"description": "Block all outbound traffic except allowlist."
		},
		"allowlist": {
			"type": "string",
			"default": "",
			"description": "Comma-separated list of allowed domains or IPs."
		}
	},
	"installsAfter": [],
	"entrypoint": "/install.sh",
	"platforms": ["linux"],
	"keywords": ["firewall", "network", "security", "outbound"]
}
