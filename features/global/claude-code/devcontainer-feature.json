{
	"id": "claude-code",
	"version": "0.1.1",
	"name": "Claude Code Agent",
	"description": "Installs the Claude Code CLI agent globally via the official npm package (@anthropic-ai/claude-code).",
	"documentationURL": "https://github.com/savvy-web/devcontainers/blob/main/docs/features/claude-code.md",
	"options": {
		"version": {
			"type": "string",
			"default": "latest",
			"description": "Claude Code CLI version (use 'latest' for the newest release)"
		}
	},
	"installsAfter": [],
	"install": {
		"shell": "./install.sh"
	},
	"platforms": ["linux", "darwin"],
	"keywords": ["claude", "ai", "agent", "global"]
}
