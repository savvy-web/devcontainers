name: Test Devcontainer Features

on:
  pull_request:

jobs:
  discover:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.features.outputs.matrix }}
    steps:
      - uses: actions/checkout@v6

      - name: Discover features
        id: features
        env:
          SKIP_REGISTRY_CHECK: "true"
        run: |
          matrix=$(node .github/scripts/topo-order.js)
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  test:
    needs: discover
    if: ${{ needs.discover.outputs.matrix != '[]' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.discover.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v6

      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli

      - name: Test feature
        id: test
        timeout-minutes: 15
        continue-on-error: true
        env:
          FEATURE_ID: ${{ matrix.id }}
        run: |
          echo "::group::devcontainer features test ${FEATURE_ID}"
          bash .github/scripts/test-feature-isolated.sh "${FEATURE_ID}"
          echo "::endgroup::"

      - name: Write step summary
        if: always()
        run: |
          {
            if [[ "${{ steps.test.outcome }}" == "success" ]]; then
              echo "## ✅ ${{ matrix.id }}"
            else
              echo "## ❌ ${{ matrix.id }}"
            fi
            echo ""
            node -e "
              try {
                const r = JSON.parse(require('fs').readFileSync('/tmp/test-results.json', 'utf8'));
                if (r.total > 0) {
                  process.stdout.write('| Scenario | Result |\n| --- | --- |\n');
                  for (const s of r.passed) process.stdout.write('| ' + s + ' | ✅ Pass |\n');
                  for (const s of r.failed) process.stdout.write('| ' + s + ' | ❌ Fail |\n');
                  process.stdout.write('\n');
                }
              } catch {}
            " 2>/dev/null
            echo "<details><summary>Test log</summary>"
            echo ""
            echo '```'
            cat /tmp/test.log 2>/dev/null || echo "No log captured"
            echo '```'
            echo ""
            echo "</details>"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Fail if tests failed
        if: steps.test.outcome == 'failure'
        run: |
          echo "::error::Tests failed for ${{ matrix.id }}. See step summary for details."
          exit 1
