name: Test Feature (Local)

on:
  workflow_dispatch:
    inputs:
      id:
        description: "Feature id (e.g. biome, rust, package-manager)"
        required: true

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Install feature
        env:
          FEATURE_ID: ${{ inputs.id }}
        run: |
          INSTALL_SCRIPT="features/${FEATURE_ID}/install.sh"
          if [[ ! -f "$INSTALL_SCRIPT" ]]; then
            echo "[ERROR] Install script not found: $INSTALL_SCRIPT" >&2
            exit 1
          fi
          chmod +x "$INSTALL_SCRIPT"
          sudo bash "$INSTALL_SCRIPT"

      - name: Run tests
        env:
          FEATURE_ID: ${{ inputs.id }}
        run: |
          TEST_SCRIPT="test/${FEATURE_ID}/test.sh"
          if [[ ! -f "$TEST_SCRIPT" ]]; then
            echo "[ERROR] Test script not found: $TEST_SCRIPT" >&2
            exit 1
          fi
          chmod +x "$TEST_SCRIPT"
          sudo bash "$TEST_SCRIPT"
